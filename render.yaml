services:
  - type: web
    name: fakeapi
    env: python
    plan: free
    region: oregon
    buildCommand: "pip install poetry && poetry install"
    startCommand: "poetry run fastapi dev fakeapi/main.py --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PORT
        value: 8000
